function introduce(name) {
    return `I'm ${name}`;
}

// After the "=>" is the code that will be executed when the function is called
const introduceArrow = name => `I'm ${name}`;
const sum = (x, y) => x + y;