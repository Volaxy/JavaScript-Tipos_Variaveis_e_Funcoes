// Type "Number"
const firstNumber = 1;
const secondNumber = 2;

const result = firstNumber + secondNumber;

console.log(result);


// Type "Float Number"
const floatNumber = 1.5;
const floatNumberWithoutZero = .5;


// Type "String"
const name = "volax";

const multiplication = name * 3;

// This doesn't work because the JavaScript can't multiply a string by a number
console.log(multiplication);

// To execute a JS script, type "node FILE_NAME" in the console