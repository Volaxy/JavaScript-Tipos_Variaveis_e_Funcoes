const userName = "Volax";
const age = 22;
const state = "ES";

// The template string is an text inside the ``, the variable is referenced with ${VARIABLE}
const text = `I'm ${userName}, my age is ${age} and my born state is ${state}`;

console.log(text);